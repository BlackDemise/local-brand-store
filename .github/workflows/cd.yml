#name: Backend CD
#
#on:
#  push:
#    branches: [ main ]
#    paths:
#      - 'backend/**'
#      - '.github/workflows/cd.yml'
#
#jobs:
#  build-and-push:
#    runs-on: ubuntu-latest
#
#    steps:
#      - uses: actions/checkout@v4
#
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v3
#
#      - name: Login to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKERHUB_USERNAME }}
#          password: ${{ secrets.DOCKERHUB_TOKEN }}
#
#      - name: Build and push
#        uses: docker/build-push-action@v5
#        with:
#          context: ./backend
#          push: true
#          tags: |
#            ${{ secrets.DOCKERHUB_USERNAME }}/lbs-backend:latest
#            ${{ secrets.DOCKERHUB_USERNAME }}/lbs-backend:${{ github.sha }}
#          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/lbs-backend:buildcache
#          cache-to: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/lbs-backend:buildcache,mode=max